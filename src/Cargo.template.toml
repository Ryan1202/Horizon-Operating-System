[package]
name = "kernel"
version = "0.0.0"
edition = "2024"

[features]
slub_debug = []

[lib]
crate-type = ["staticlib"]

[profile.dev]
panic = "abort"
opt-level = 0

[profile.release]
panic = "abort"
