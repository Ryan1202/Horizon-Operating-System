name: C/C++ CI

on:
  push:
    branches: [ "master", "GUI" ]
  pull_request:
    branches: [ "master", "GUI" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3, ilammy/setup-nasm@v1.4.0
    - name: make
      run: make
