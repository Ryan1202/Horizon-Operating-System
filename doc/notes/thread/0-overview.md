# 基本原则

- 尽量避免在设计上就定义父子线程的关系，也就是说不可能出现全局的父线程
- 逻辑上的父子线程也没有强制的生命周期的包含关系，除非手动限制（如使用 `join` ）
- 线程的销毁时机通过引用计数决定，可以通过持有线程的强引用从而延迟子线程的销毁从而获取到退出信息
- 为避免持有另一个线程强引用的线程未释放引用先退出而导致泄漏，可以通过再次封装进行管理，但不在内核线程管理范围内
- 退出的线程由下一个被调度的线程清理